
    // typescript primitive data types

    let n : number; // integers, floats, etc.
    let b : boolean; // true, false
    let s : string; // strings
    let y : symbol; //
    let e : null; // an empty or invalid value type
    let u : undefined; // an uninitialized variable type

    // object data types
    let arr : Array<any> // list type (supports generics)
    //let tup : Tuple<any> // tuple type (supports generics)

    let a : any // any can be any object type at all
    let o : object // object type
    let v : void // void is a void object